package com.afriland.ticket2cash.voucher;

public enum VoucherUsageMode {

    /** Can only be consumed once. */
    SINGLE_USE,

    /** Can be consumed multiple times up to maxUses. */
    MULTI_USE
}
